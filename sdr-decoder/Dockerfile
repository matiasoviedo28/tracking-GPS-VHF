# Placeholder — desarrollo del pipeline SDR/LRRP a cargo del equipo de
# decodificación, en paralelo (ver ARQUITECTURA.md secciones 2 y 10).
# Reemplazar por la imagen real una vez que el prototipo standalone sea
# estable.

FROM python:3.12-slim

WORKDIR /app

CMD ["echo", "sdr-decoder: pendiente de implementación"]
